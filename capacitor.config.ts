import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.madebyjamshid.fresherjobs',
  appName: 'Fresher Jobs UAE',
  webDir: 'dist'
};

export default config;
